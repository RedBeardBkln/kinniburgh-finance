TASK: Extend the app's forecast view with daily/weekly/monthly/quarterly horizon options, combining the existing schedule-based cash-flow forecast with the trend-based per-tag spend projection (both already built and merged).

CONTEXT: This is Tier 1, item 2 of an agreed product roadmap toward the platform's north star: "automate as much financial work as possible" via forecast projections at every cadence (daily/weekly/monthly/quarterly) based on historical spending + budgets, keeping the household informed. Two forecasting primitives already exist and are already reviewed/merged:
- `lib/forecast.ts` — schedule-based: projects a day-by-day account balance forward from KNOWN recurring transfers/bills/income (interest-free of actual spending trends). Read this file fully, including `buildAccountForecast`, `DayForecast`, `findBreachDays`.
- `lib/spend-forecast.ts` — trend-based: given a tag's trailing-months monthly spend history + current period-to-date spend, projects the likely end-of-period total for that tag via `projectPeriodEndSpend()`. Read this file fully.
- `lib/budget-pace.ts` — a pure decision layer built on top of spend-forecast for the notification system (checkBudgetPace) — read this too, as a precedent for how spend-forecast gets consumed, but note its job (fire/suppress an alert) is different from this task's job (render a view).

Also read the EXISTING forecast page fully before planning: `app/forecast/page.tsx` and `app/forecast/loading.tsx`, plus whatever server action(s) currently feed it (grep for where `buildAccountForecast` or `generateTransferOccurrences`/`generateIncomeOccurrences`/`generateBillOccurrences`/`generateCardStatementPayment` from lib/forecast.ts are actually called from — likely an actions/*.ts file; read that file fully too). Understand exactly what the current page shows (per your findings in the last multi-agent session, it currently shows a day-by-day account balance forecast with breach-day warnings and lets the user configure income/transfer schedules) before planning changes, so you extend it rather than guessing at its current shape.

SCOPE — read carefully, this task is intentionally bounded:
- IN SCOPE: Personal bucket account-balance forecasting (existing) PLUS per-tag/category spend-pace forecasting (new, using lib/spend-forecast.ts) surfaced at daily/weekly/monthly/quarterly rollups. The "quarterly" horizon here means: roll up the existing daily projection into weekly/monthly/quarterly buckets for display (e.g., "end of week 1: $X", "end of month: $Y", "end of quarter: $Z") — it does NOT mean building a full business quarterly P&L or estimated-tax-payment forecast; that is a SEPARATE, larger roadmap item (Tier 1 item 3) explicitly out of scope for this task. Do not build business-entity quarterly tax projections here.
- IN SCOPE: showing spend-forecast projections (which tags are pacing over/under budget, and by how much, projected to period end) somewhere on this page or a clearly-linked new section, using the already-built `projectPeriodEndSpend()`.
- OUT OF SCOPE: business entity forecasting, estimated tax payments, changing lib/forecast.ts's or lib/spend-forecast.ts's public APIs (both are stable, reviewed dependencies — flag in the plan as a risk if you find either's API genuinely insufficient, don't silently change them), Prisma schema changes, new dependencies.

DESIGN QUESTIONS FOR YOU TO RESOLVE (make concrete decisions, don't leave these open for the Coder):
1. What does "daily/weekly/monthly/quarterly" mean concretely for the ACCOUNT BALANCE side (already driven by `buildAccountForecast`'s day-by-day output)? E.g., a horizon selector that changes the display granularity/grouping of the same underlying day-by-day data (roll daily points up into weekly/monthly/quarterly summary rows), vs. changing the forecast's date range. Decide and specify exactly what changes in the UI per horizon.
2. Where does the SPEND-PACE side fit into the page? A new tab/section? Which tags get shown (all budgeted tags for the current period, or only ones with a meaningful projection)? What does "quarterly" mean for spend-pace, given `projectPeriodEndSpend` is fundamentally a within-one-budget-period (monthly) projection — does quarterly spend-pace mean summing three months of monthly budgets/projections, or is quarterly spend-pace out of scope and only weekly/monthly apply to that side, with quarterly being account-balance-only? Decide explicitly and justify.
3. Is this server-rendered (like the current page, which appears to be an RSC page) or does it need client interactivity for a horizon-switcher tab/dropdown? Follow whatever pattern the existing page.tsx actually uses (check if it's "use client" or a server component with a client sub-component for the interactive parts) — match the codebase's actual pattern, don't invent a new one.
4. Data-fetching shape: what new server action(s), if any, need to be added (e.g., to actions/dashboard.ts, actions/reports.ts, or a new actions/forecast.ts if none exists) to supply spend-pace data to the page, following this codebase's existing `requireAuth()`-first server action pattern?

REQUIREMENTS:
- Follow CLAUDE.md conventions: TypeScript strict, Decimal for money, no `any`.
- Ground rule 8 (observational, never advice): any spend-pace display must read as an observation/projection, not advice — consistent with the notification copy already shipped in lib/notifications.ts's checkBudgetPace for tone reference.
- Testing: any new PURE logic (e.g., a rollup/aggregation function that groups daily forecast points into weekly/monthly/quarterly buckets) must be unit tested in lib/__tests__/ following existing patterns. UI/page-component code and server actions are not expected to have unit tests in this codebase's established pattern (no DB in unit tests) — confirm this against how the EXISTING app/forecast/page.tsx and its backing action(s) are or aren't tested, and follow the same line.
- Definition of done: pnpm typecheck, pnpm lint, and pnpm test (full suite) all pass clean. Current baseline is 317/317 tests across 29 files — new tests should add to that with zero regressions. NOTE: this task touches UI, and this pipeline's Coder/Tester/Reviewer agents do not have browser access — say explicitly in the plan that a human (or a separate browser-driving step) should visually verify the rendered page before this is considered fully done, since typecheck/lint/test cannot catch a broken layout or a horizon selector that doesn't actually change what's displayed.

Produce a concrete, scoped implementation plan: exact new function signatures (rollup/aggregation logic), the page/component structure (what's new vs. modified, server vs. client boundaries), the server action(s) to add, and the specific test cases for any new pure logic — detailed enough that the Coder doesn't have to make further design decisions.
