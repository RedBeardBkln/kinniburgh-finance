Build GL-code auto-assignment for business transactions, driven by a manually-configured tag→GL-code mapping per entity, plus a retroactive backfill and going-forward automation.

CONTEXT: This directly unblocks the business P&L/quarterly-forecast features (already built in prior tasks — lib/reports.ts's computePL, lib/business-quarter-forecast.ts) which currently return all-zero results because NO business transaction has ever had a GL code assigned. This was discovered and confirmed via direct database inspection during a prior task (not fabricated/assumed — verified live): every GlCode row for both business entities is correctly typed (asset/liability/equity/revenue/expense per actions/gl-codes.ts's GL_TYPES enum), but `db.transaction.count({where:{glCodeId:{not:null}}})` returns 0 for both entities. Separately, `lib/reports.ts`'s `computePL` has its own independent bug — it checks `gl.type === "income"` but the correct/actual enum value is `"revenue"` — flag this in the plan as a one-line fix to include (change `"income"` to `"revenue"` in the two places `computePL` checks `gl.type`), since it must be fixed for GL-coded transactions to actually show up as revenue once this task's assignment logic starts working.

THE OWNER'S REQUESTED DESIGN (already decided, build to this): map each Tag to a GL code, per business entity (the same tag might need a different GL code for Sudden Valley vs EK Consulting, since GL charts differ per entity). When a transaction gets tagged and its entity has a mapping for that tag, automatically set the transaction's GL code to match. This needs to work both retroactively (backfill all existing already-tagged business transactions once mappings exist) and going forward (new transactions get auto-assigned as they're tagged).

Full task brief (including read-first file list, design questions to resolve, and requirements) was provided in full to the Planner in the invoking message. Key points preserved here:

- Read fully before planning: prisma/schema.prisma (Tag, TransactionTag, GlCode, Transaction, Entity, AuditLog); actions/gl-codes.ts (assignGlCode); lib/reports.ts's computePL; all 7 TransactionTag-creation call sites (actions/import.ts, actions/tag-rules.ts, actions/transactions.ts x3, lib/notifications.ts, lib/plaid-sync.ts); components/tag-rules/retroactive-rule-modal.tsx + actions/tag-rules.ts's preview/apply pattern; app/business/[slug]/gl/page.tsx + components/business/gl-page-client.tsx (and check .claude/pipeline/quarterly-business-forecast/02-implementation.md for a previously-flagged "latent bug" note about the GL type dropdown).
- Conflict rule (owner-stated): if a transaction's tags map to more than one distinct GL code for its entity, do NOT auto-assign — leave for manual review. Only auto-assign on exactly one distinct resolved GL code.
- Resolver must be a single shared, pure, unit-testable function (e.g. lib/gl-code-resolver.ts) used by all 7 call sites and the backfill.
- Requires a Prisma migration (first schema change this session) — write schema.prisma + migration file only; do NOT run `prisma migrate deploy`/`db push` against the live database as part of planning or building. `prisma generate` (client-only, no DB connection) is fine and required before typecheck/test.
- Testing: resolver must be pure/unit-testable, no DB in unit tests. Baseline before this task: 351/351 tests across 31 files (verified live by the Planner via `pnpm test`) — must stay green with additions only.
- UI touches (mapping management page, backfill preview/apply flow) need human visual verification — no pipeline agent has browser access.
- Planner asked to make an explicit call on one Coder pass vs. splitting the work, and justify it.
